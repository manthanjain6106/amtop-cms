DATABASE_URI=mongodb://127.0.0.1/your-database-name
PAYLOAD_SECRET=YOUR_SECRET_HERE

# API automation (scripts, crawlers). Generate API key in Admin: Users → edit user → API Key
PAYLOAD_API_URL=http://localhost:3000
PAYLOAD_API_KEY=3cfb8a6c-87c9-4774-bd58-cd3a4bc33e56

# Media → Google Cloud Storage (required on Vercel; optional locally)
GOOGLE_CLOUD_BUCKET=
GOOGLE_CLOUD_PROJECT_ID=
# Local: path to service account JSON
# GOOGLE_CLOUD_KEY_FILE=credentials/your-key-file.json
# Vercel: base64-encoded JSON key (Vercel env convention)
# GCP_KEYFILE_BASE64=<base64 of key file>
# Optional: custom base URL for media (e.g. CDN)
# GOOGLE_CLOUD_BASE_URL=

